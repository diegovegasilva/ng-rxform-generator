export enum errorMsg {
  REQUIRED_FIELD = 'Mandatory Field',
  INVALID_EMAIL = 'Invalid Field',
  INVALID_MINLENGTH = 'Invalid min length',
  INVALID_MAXLENGTH = 'Invalid max length',
  INVALID_PATTERN = 'Wrong pattern'
}
